/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,  // Enables React Strict Mode and Fast Refresh
    swcMinify: true,  // This helps with performance in production, optional
  };
  
  export default nextConfig;
  